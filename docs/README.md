# Project API Documentation

This documentation covers all public APIs, functions, signals, exported properties, and components in the project. It is organized by domain: utils, scenes, UI components, resources, and game flow.

- See `api/utils.md` for utility APIs.
- See `api/game_flow.md` for BaseGame and BaseGameScene flow.
- See `api/camera.md` for CameraMan.
- See `api/ui/` for UI components.
- See `api/scenes/` for game scenes and actors.
- See `api/resources/` for resource types and data models.

